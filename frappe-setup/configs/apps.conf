# apps.conf - Default configuration for Frappe Setup Wizard
# Edit these values instead of touching the scripts.

DEFAULT_VERSION=version-15
DEFAULT_NODE=18
DEFAULT_APPS="erpnext hrms"
WORKSPACE=$HOME/Projects
